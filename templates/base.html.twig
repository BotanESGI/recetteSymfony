<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Reectte sujet{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    {% block stylesheets %}{% endblock %}
</head>
<body class="bg-gray-100 text-gray-900">

<header class="bg-white shadow">
    <nav class="max-w-7xl mx-auto px-4 py-4">
        <ul class="flex space-x-4">
            <li><a href="{{ path('home_page') }}" class="text-blue-500 hover:text-blue-700">Accueil</a></li>
            <li><a href="{{ path('recipe_page') }}" class="text-blue-500 hover:text-blue-700">Recettes</a></li>
            {% if app.user %}
                <li><a href="{{ path('logout') }}" class="text-blue-500 hover:text-blue-700">Mon Compte</a></li>
                <li><a href="{{ path('logout') }}" class="text-blue-500 hover:text-blue-700">Déconnexion</a></li>
            {% else %}
                <li><a href="{{ path('login') }}" class="text-blue-500 hover:text-blue-700">Connexion</a></li>
                <li><a href="{{ path('register') }}" class="text-blue-500 hover:text-blue-700">Inscription</a></li>
            {% endif %}
        </ul>
    </nav>
</header>

<main class="max-w-7xl mx-auto p-6">
    {% block body %}{% endblock %}
</main>

<footer class="bg-gray-800 text-white py-4">
    <div class="max-w-7xl mx-auto text-center">
        <p>&copy; {{ "now"|date("Y") }} Footer Sujet Recette</p>
    </div>
</footer>

{% block javascripts %}{% endblock %}
</body>
</html>
